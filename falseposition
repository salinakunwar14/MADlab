#include<stdio.h>
#include<math.h>
double f(double x){  
return e^x-1;
}
void falseposition(double a, double b)
{
	double c;
	while (fabsf(b-a)>1e-4);{
	c=(a*f(b)-b*f(a)/f(b)-f(a));

if (f(a)*f(c)>0)?(b=c):(a=c);
}
printf("required root is :%f/n";c);
}
int main(){
	falseposition(0,5);
	return 0;
}
